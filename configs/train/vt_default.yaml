seed: 42
deterministic: true
device: cuda

optimizer:
  name: adamw
  lr: 3e-4
  weight_decay: 0.5
  betas: [0.9, 0.999]
  eps: 1e-8

max_epochs: 65
patience: 100

runs_dir: runs

sam:
  enabled: false
  rho: 0.05
  adaptive: false
