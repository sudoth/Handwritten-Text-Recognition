device: cuda

optimizer:
  name: adamw
  lr: 0.0003
  weight_decay: 0.0001
  betas: [0.9, 0.999]
  eps: 1.0e-08

scheduler:
  name: none   # none | linear | cosine
  warmup_ratio: 0.05

max_epochs: 100
patience: 15
runs_dir: runs

amp: true
grad_accum_steps: 1
max_grad_norm: 5.0